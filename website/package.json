{
  "name": "amazonebooks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "python build_pseo.py",
    "dev": "vite",
    "build": "python build_pseo.py && vite build",
    "preview": "vite preview",
    "deploy": "python build_pseo.py && vite build && wrangler pages deploy dist --project-name amazonebooks"
  },
  "devDependencies": {
    "vite": "^8.2.0",
    "wrangler": "^4.122.0"
  }
}
